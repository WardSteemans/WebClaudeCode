// ── Claude Code slash commands ──
// Re-exported from shared — all definitions live in @cc-gui/shared now.
// Keep this file as a re-export for internal consumers.

export type { SlashCommand } from '@cc-gui/shared';
export { SLASH_COMMANDS } from '@cc-gui/shared';
